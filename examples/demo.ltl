// examples/demo.ltl — runnable demo for ltl_patch

import std::io
use ltl_patch as lib

fn main() {
    io::println("ltl_patch demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}