// ltl-patch/main.ltl
//
// Apply unified diffs with fuzz matching and reject files.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-patch v0.1.0")
    io::println("Apply unified diffs with fuzz matching and reject files.")
    0
}
